export function getMonthlyInsights(data) {
    const rules = [
        {
            check: d => d.avgCustomerPerDay >= 3,
            text: "🚀 ลูกค้าเฉลี่ยต่อวันอยู่ในเกณฑ์ดีมาก"
        },
        {
            check: d => d.barberFromGuarantee > d.barberOver,
            text: "📉 รายได้ช่างเดือนนี้ยังพึ่งพาเงินประกันเป็นหลัก"
        },
        {
            check: d => d.zeroCustomerDays > 0,
            text: `😴 มีวันว่างที่ไม่มีลูกค้าเลย ${data.zeroCustomerDays} วัน`
        },
        {
            check: d => d.offDays >= 6,
            text: "📅 เดือนนี้มีวันหยุดค่อนข้างเยอะ"
        }
    ];

    // กรองเอาเฉพาะข้อความที่ตรงตามเงื่อนไข (แทนการใช้ eval)
    return rules.filter(r => r.check(data)).map(r => r.text);
}
